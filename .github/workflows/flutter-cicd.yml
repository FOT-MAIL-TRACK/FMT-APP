name: Flutter CI for FMT-APP

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.13.0'

    - name: Install dependencies
      run: |
        cd your_flutter_project_directory
        flutter pub get

    - name: Run unit tests
      run: |
        cd your_flutter_project_directory
        flutter test

    - name: Build APK
      run: |
        cd your_flutter_project_directory
        flutter build apk --release

    - name: Move APK to root directory
      run: |
        mv your_flutter_project_directory/build/app/outputs/flutter-apk/app-release.apk .

    - name: Upload APK as artifact
      uses: actions/upload-artifact@v3
      with:
        name: FMT-APP-APK
        path: app-release.apk

